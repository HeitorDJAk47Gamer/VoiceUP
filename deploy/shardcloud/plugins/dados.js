module.exports = { id: 'dados', name: 'Dados RPG', version: 'beta.1', description: 'Rola d20, 2d6+3 e expressões parecidas.', onTextMessage({ text, room, textChannel, user, api, plugin }) { const match = String(text).match(/(?:^|\s)(\d{0,3})d(\d{1,4})(?:\s*([+-])\s*(\d{1,5}))?(?=\s|$)/i); if (!match) return; const dice = Math.max(1, Number(match[1] || 1)), faces = Number(match[2]), modifier = match[3] ? (match[3] === '-' ? -1 : 1) * Number(match[4]) : 0; if (dice > 100 || faces < 2 || faces > 1000 || Math.abs(modifier) > 10000) return; const rolls = Array.from({ length: dice }, () => Math.floor(Math.random() * faces) + 1), total = rolls.reduce((sum, value) => sum + value, 0) + modifier, expression = `${dice === 1 ? '' : dice}d${faces}${modifier ? (modifier > 0 ? ` + ${modifier}` : ` - ${Math.abs(modifier)}`) : ''}`; api.systemMessage(room, textChannel, `🎲 ${user.name} rolou ${expression}: [${rolls.join(', ')}]${modifier ? ` ${modifier > 0 ? '+' : '-'} ${Math.abs(modifier)}` : ''} = ${total}`, { name: 'Dados RPG', color: '#e8b65a', pluginId: plugin.id }); } };
